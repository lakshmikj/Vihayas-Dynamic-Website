<section>
    <div class="container p-5">
        <table class="table mt-5 pt-5" style="padding-top: 10rem;" id="allOrder">
            <thead class="tableHead">
                <tr>
                    <th scope="col">Date</th>
                    <th>Address</th>
               
                    <th>Pincode</th>
                    <th>Mobile</th>
                    <th scope="col">Amount</th>
                    <th scope="col">Payment</th>
                    <th>Status</th>
                    <th>Products</th>
                </tr>
            </thead>
            <tbody>
                {{#each orders}}
                    <tr>
                        <td>{{this.date}}</td>
                    
                        <td>{{this.deliveryDetails.address}}</td>
                        <td>{{this.deliveryDetails.pincode}}</td>
                        <td>{{this.deliveryDetails.mobile}}</td>
                        <td>₹{{this.totalAmount}}.00</td>
                        <td>{{this.paymentMethode}}</td>
                        <td>{{this.status}}</td>

                        <td>
                            <a href="/view-ordered-products/{{this._id}}" class="btn btn-primary cartButton">View Products</a>
                        </td>

                    </tr>
                {{/each}}
            </tbody>
        </table>

    </div>
</section>
<script>
    $(function(){
        $('#allOrder').DataTable();
    })
</script>